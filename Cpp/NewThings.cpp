#include<bits/stdc++.h>
using namespace std;

int main(){

    // When input given in a line
    // Then instead of taking it as string using getline and convert into integer
    // we can also use th below method 
    int arr[10];
    int i = 0;
    while((cin >> arr[i])){
        i++;
    }

    return 0;
}
